import { describe, it, expect } from 'vitest';

// Example unit test for a utility function
describe('Tile utility', () => {
  it('should exist', () => {
    // Basic test to check if test framework works
    expect(1 + 1).toBe(2);
  });
});